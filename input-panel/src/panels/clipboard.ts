import { Panel } from "../types/panel";

export const clipboardPanel: Panel = {
    id: "clipboard",
    name: "Clipboard",
    buttons: [
        // These will be dynamically populated in main.ts
    ]
};
